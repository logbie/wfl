// Simple Random Test

// Test random function
// Note: The random function is available at runtime
// When used directly with 'with', it displays "[NativeFunction]" instead of calling the function

// Display the random function reference
display "Random function: " with random

// Test basic random number generation
store random_value as random
display "Random value: " with random_value
