// Random and Time Test

// Test random function by storing its result
store random_value as random
display "Random number: " with random_value

// Test today function (current_date is not available)
store current_date_value as today
display "Current date: " with current_date_value

// Test multiple random values
store random_value2 as random
display "Another random number: " with random_value2

store random_value3 as random
display "Third random number: " with random_value3
