// Time module test script

// Test current date and time
store today_value as today
display "Current date: " with today_value

store now_value as now
display "Current time: " with now_value

store datetime_now_value as datetime_now
display "Current datetime: " with datetime_now_value

// Test basic date and time display
store date_value as today
display "Date value: " with date_value

store time_value as now
display "Time value: " with time_value

// Test basic date and time values
store year as 1990
store month as 1
store day as 15
display "Birthday components - Year: " with year with ", Month: " with month with ", Day: " with day

store hour as 14
store minute as 30
display "Meeting time components - Hour: " with hour with ", Minute: " with minute

// Test basic date operations
store days_to_add as 1
display "Days to add: " with days_to_add

store days_to_add_week as 7
display "Days to add for week: " with days_to_add_week

// Test basic string values
store date_string as "2025-12-25"
store date_format as "%Y-%m-%d"
display "Date string: " with date_string
display "Date format: " with date_format

store time_string as "12:00"
store time_format as "%H:%M"
display "Time string: " with time_string
display "Time format: " with time_format
