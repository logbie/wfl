// Simple test with just a function definition
define action called "greet":
    needs:
        name as text
    gives back:
        message as text
    does:
        store greeting as "Hello "
        give back greeting
end action 